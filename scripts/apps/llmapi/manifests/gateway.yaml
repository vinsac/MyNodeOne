apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
  namespace: llmapi
data:
  REDIS_URL: "redis://redis:6379/0"
  POSTGRES_HOST: "llmapi-postgres"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "llmapi"
  POSTGRES_USER: "llmapi"
  VLLM_URLS: "http://vllm-0.vllm:8000"
  LLAMACPP_URL: "http://llamacpp:8080"
  EMBEDDING_URL: "http://embedding:8080"
  OLLAMA_URL: "http://ollama:11434"
  # Model names for discovery (llama.cpp/embedding don't have /v1/models)
  LLAMACPP_MODEL_NAME: "llama-cpu"
  EMBEDDING_MODEL_NAME: "embedding"
  # Default quotas
  DEFAULT_REQUESTS_PER_MINUTE: "60"
  DEFAULT_TOKENS_PER_DAY: "100000"
  # Lazy loading (Ollama backend)
  LAZY_LOAD_ENABLED: "true"
  LAZY_LOAD_BACKEND: "ollama"
  AUTO_DOWNLOAD: "false"
  # Horizontal scaling: route to least-loaded backend (GPU1 → GPU2 → CPU)
  # When enabled, all chat backends are treated as equivalent
  HORIZONTAL_SCALING: "true"
  MAX_INFLIGHT_PER_BACKEND: "32"
  # Rate limiting
  CONCURRENCY_PER_GPU: "1"
  CONCURRENCY_PER_KEY_DEFAULT: "1"
  CONCURRENCY_PER_EMBEDDING_REPLICA: "4"
  CONCURRENCY_LEASE_TTL_SECONDS: "600"
  DEFAULT_EMBEDDING_REQUESTS_PER_MINUTE: "60"
  DEFAULT_EMBEDDING_TOKENS_PER_MINUTE: "40000"
  # Admin password (change this!)
  ADMIN_PASSWORD: "admin"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: llmapi
  labels:
    app: llmapi-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: llmapi-gateway
  template:
    metadata:
      labels:
        app: llmapi-gateway
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      priorityClassName: mynodeone-app
      serviceAccountName: gateway
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: gateway
        image: ghcr.io/mynodeone/llmapi-gateway:latest
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: llmapi-db
              key: postgres-password
        envFrom:
        - configMapRef:
            name: gateway-config
        resources:
          requests:
            memory: "32Mi"
            cpu: "50m"
          limits:
            memory: "2Gi"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: llmapi
  namespace: llmapi
  annotations:
    mynodeone.io/subdomain: "llmapi"
spec:
  selector:
    app: llmapi-gateway
  ports:
  - port: 80
    targetPort: 8000
    name: http
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: llmapi-internal
  namespace: llmapi
spec:
  selector:
    app: llmapi-gateway
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
